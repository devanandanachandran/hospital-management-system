import { Heart, Brain, Bone, Baby, Eye, Stethoscope, Pill, Activity } from 'lucide-react';

// Keys here must match the `icon` string stored on each Department document
export const DEPARTMENT_ICONS = {
  Stethoscope,
  Heart,
  Brain,
  Bone,
  Baby,
  Eye,
  Pill,
  Activity
};

export const DEPARTMENT_ICON_KEYS = Object.keys(DEPARTMENT_ICONS);
